function isIsogram(str){
    let newStr = str.every()
  }

const isIsogram = str.every((total) => total != total)

  //.every()
// const everyBobby = studentNames.every((student) => student == 'James')
// console.log('Eveeryones bobby?', everyBobby)
