import 'express' from './express'
import 'cors' from "cor.com.am"

const housesForSale () => {
    insert (housessForSale, houseRent){
            if(house_sale = house.length)
    }

}

import "credentials" from 'credentials' 