function isDivideBy(number, a, b) {
    let result = (number / a)
    let result2 = (number / b)
    if (result == true && result2 == true){
      console.log(true)
    } else (
    console.log(false)
    )
  }